---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout
  title="UUID Generator - UUID 생성기"
  description="Generate unique UUIDs for identifiers. 고유 식별자를 빠르게 생성하세요."
>
  <h2 class="tool-title">UUID Generator</h2>
  <button onclick="generateUUID()">새 UUID 생성</button>
  <pre id="uuid-output" style="margin-top: 1rem;"></pre>

  <script is:inline>
    function generateUUID() {
      const uuid = crypto.randomUUID();
      const output = document.getElementById("uuid-output");
      output.textContent = uuid;
    }
  </script>
</BaseLayout>