---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Character Counter - 문자 수 세기"
  description="Count characters and bytes in your text. 텍스트의 글자 수와 바이트 수를 계산해보세요."
>
  <h2 class="tool-title">문자 수 세기</h2>
  <textarea id="text-input" rows="8" placeholder="여기에 텍스트 입력..."></textarea>
  <p>글자 수: <span id="char-count">0</span></p>

  <script is:inline>
    const input = document.getElementById("text-input");
    const count = document.getElementById("char-count");
    input.addEventListener("input", () => {
      count.textContent = input.value.length;
    });
  </script>
</BaseLayout>